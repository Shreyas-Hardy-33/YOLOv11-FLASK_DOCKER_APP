Simple flask app that uses YOLOv11n model to detect number of potholes in an image.
A user can upload their image , which is processed by the python script and new markers are displayed on a map.

To use this app:

Download ultralytics,flask libraries:
        pip install flask ultralytics opencv-python requests werkzeug
